<div class="auth-container">
  <div class="auth-form-container">
    <!-- Header Section with Logo -->
    <div class="auth-header">
      <div class="auth-logo">
        <%= image_tag "logo-rd.png", alt: I18n.t(:logo_rd_alt_image) %>
      </div>
      <h2 class="auth-heading"><%= I18n.t(:heading, scope: %i[devise sessions new]) %></h2>
      <p class="auth-subtitle"><%= I18n.t(:subtitle, scope: %i[devise sessions new]) %></p>
    </div>

    <%= render "devise/shared/error_messages", resource: resource %>

    <%= simple_form_for(resource, as: resource_name, url: user_session_path, html: { class: "auth-form" }) do |f| %>
      <!-- Name Field with Icon -->
      <div class="form-group form-group-with-icon">
        <label class="form-label" for="user_name">
          <i class="bi bi-person me-1"></i>
          <%= I18n.t(:name, scope: %i[activerecord attributes user]) %>
        </label>
        <div class="form-icon">
          <i class="bi bi-person"></i>
        </div>
        <%= f.input :name,
                  required: false,
                  autofocus: true,
                  placeholder: I18n.t(:name_placeholder, scope: %i[devise sessions new]),
                  input_html: {
                    autocomplete: "name",
                    class: "form-control",
                    id: "user_name"
                  },
                  label: false %>
      </div>

      <!-- Password Field with Icon -->
      <div class="form-group form-group-with-icon">
        <label class="form-label" for="user_password">
          <i class="bi bi-lock me-1"></i>
          <%= I18n.t(:password, scope: %i[activerecord attributes user]) %>
        </label>
        <div class="form-icon">
          <i class="bi bi-lock"></i>
        </div>
        <%= f.input :password,
                  required: false,
                  placeholder: I18n.t(:password_placeholder, scope: %i[devise sessions new]),
                  input_html: {
                    autocomplete: "current-password",
                    class: "form-control",
                    id: "user_password"
                  },
                  label: false %>
      </div>

      <!-- Remember Me Checkbox -->
      <% if devise_mapping.rememberable? %>
        <div class="form-check">
          <%= f.input :remember_me,
                    as: :boolean,
                    input_html: { class: "form-check-input" },
                    label_html: { class: "form-check-label" },
                    label: I18n.t(:remember_me, scope: %i[devise sessions new]) %>
        </div>
      <% end %>

      <!-- Submit Button -->
      <div class="form-actions">
        <%= f.button :submit, I18n.t(:submit, scope: %i[devise sessions new]), class: "btn btn-primary auth-btn" %>
      </div>
    <% end %>

    <!-- Additional Links -->
    <div class="auth-links">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
