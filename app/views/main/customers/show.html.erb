<div class="customers-show-container">
  <!-- Success/Notice Alert -->
  <% if notice %>
    <div class="alert alert-success alert-dismissible fade show customers-show-alert" role="alert">
      <i class="bi bi-check-circle-fill"></i>
      <%= notice %>
      <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
  <% end %>

  <!-- Page Header -->
  <div class="customers-show-header">
    <div class="customers-show-title-section">
      <h1 class="customers-show-title">
        <i class="bi bi-person-fill customers-show-title-icon"></i>
        <span class="customers-show-title-name"><%= @customer.name %></span>
      </h1>
      <p class="customers-show-subtitle">Visualize e gerencie as informações do cliente</p>
    </div>
    <div class="customers-show-actions">
      <%= link_to main_customers_path, class: "customers-show-btn-secondary" do %>
        <i class="bi bi-arrow-left customers-show-btn-icon"></i>
        <%= I18n.t(:return) %>
      <% end %>
      <%= link_to edit_main_customer_path(@customer), class: "customers-show-btn-primary" do %>
        <i class="bi bi-pencil customers-show-btn-icon"></i>
        <%= I18n.t(:edit) %>
      <% end %>
      <%= status_toggle_button(@customer) %>
    </div>
  </div>

  <!-- Tabs Navigation -->
  <div class="customers-show-tabs-card">
    <ul class="customers-show-nav-tabs" id="customerShowTabs" role="tablist">
      <li class="customers-show-nav-item" role="presentation">
        <button class="customers-show-nav-link active" id="profile-tab" data-bs-toggle="tab" data-bs-target="#profile-tab-pane" type="button" role="tab" aria-controls="profile-tab-pane" aria-selected="true">
          <i class="bi bi-person-circle customers-show-tab-icon"></i>
          <%= I18n.t(:profile) %>
        </button>
      </li>
      <li class="customers-show-nav-item" role="presentation">
        <button class="customers-show-nav-link" id="purchases-tab" data-bs-toggle="tab" data-bs-target="#purchases-tab-pane" type="button" role="tab" aria-controls="purchases-tab-pane" aria-selected="false">
          <i class="bi bi-cart-fill customers-show-tab-icon"></i>
          <%= I18n.t(:purchases) %>
        </button>
      </li>
      <li class="customers-show-nav-item" role="presentation">
        <button class="customers-show-nav-link" id="documents-tab" data-bs-toggle="tab" data-bs-target="#documents-tab-pane" type="button" role="tab" aria-controls="documents-tab-pane" aria-selected="false">
          <i class="bi bi-file-earmark-text customers-show-tab-icon"></i>
          <%= I18n.t(:documents) %>
        </button>
      </li>
    </ul>

    <div class="customers-show-tab-content" id="customerShowTabContent">
      <!-- Profile Tab -->
      <div class="customers-show-tab-pane fade show active" id="profile-tab-pane" role="tabpanel" aria-labelledby="profile-tab" tabindex="0">
        <div class="customers-show-profile-card">
          <div class="customers-show-profile-header">
            <h3 class="customers-show-profile-title">
              <i class="bi bi-info-circle customers-show-profile-icon"></i>
              Informações do Cliente
            </h3>
          </div>
          <div class="customers-show-profile-content">
            <div class="customers-show-profile-avatar">
              <%= image_tag(
                @customer.avatar.attached? ? url_for(@customer.avatar) : asset_path("img-perfil.png"),
                alt: I18n.t(:alt_image, scope: %i[activerecord], model: @customer.model_name.human),
                class: "customers-show-avatar-image"
              ) %>
            </div>
            <div class="customers-show-profile-details">
              <div class="customers-show-detail-group">
                <label class="customers-show-detail-label">
                  <i class="bi bi-person customers-show-detail-icon"></i>
                  <%= I18n.t(:name, scope: %i[activerecord attributes customer]) %>
                </label>
                <span class="customers-show-detail-value"><%= @customer.name %></span>
              </div>
              <div class="customers-show-detail-group">
                <label class="customers-show-detail-label">
                  <i class="bi bi-hash customers-show-detail-icon"></i>
                  <%= I18n.t(:matriculation, scope: %i[activerecord attributes customer]) %>
                </label>
                <span class="customers-show-detail-value"><%= @customer.matriculation %></span>
              </div>
              <div class="customers-show-detail-group">
                <label class="customers-show-detail-label">
                  <i class="bi bi-toggle-<%= @customer.active? ? 'on' : 'off' %> customers-show-detail-icon"></i>
                  <%= I18n.t(:status, scope: %i[activerecord attributes customer]) %>
                </label>
                <span class="customers-show-status-badge customers-show-status-<%= @customer.active? ? 'active' : 'inactive' %>">
                  <%= I18n.t(@customer.active? ? :active : :inactive) %>
                </span>
              </div>
            </div>
          </div>
        </div>
      </div>
      <!-- Purchases Tab -->
      <div class="customers-show-tab-pane fade" id="purchases-tab-pane" role="tabpanel" aria-labelledby="purchases-tab" tabindex="0">
        <div class="customers-show-purchases-card">
          <div class="customers-show-purchases-header">
            <h3 class="customers-show-purchases-title">
              <i class="bi bi-cart-check customers-show-purchases-icon"></i>
              <%= I18n.t(:recent_purchases) %>
            </h3>
          </div>
          <div class="customers-show-purchases-content">
            <% if @customer.sales.size > 0 %>
              <div class="customers-show-sales-grid">
                <% @customer.sales.order(created_at: :desc).each do |sale| %>
                  <div class="customers-show-sale-card">
                    <div class="customers-show-sale-header">
                      <div class="customers-show-sale-date">
                        <i class="bi bi-calendar3 customers-show-sale-icon"></i>
                        <%= sale.created_at.strftime("%d/%m/%Y") %>
                      </div>
                      <div class="customers-show-sale-total">
                        <i class="bi bi-currency-dollar customers-show-sale-icon"></i>
                        <%= number_to_currency(sale.total_price) %>
                      </div>
                    </div>
                    <div class="customers-show-sale-actions">
                      <button class="customers-show-btn-action show-cart"
                              data-show-text="<%= I18n.t(:show_cart) %>"
                              data-hide-text="<%= I18n.t(:hide_cart) %>">
                        <i class="bi bi-eye customers-show-btn-icon"></i>
                        <%= I18n.t(:show_cart) %>
                      </button>
                    </div>
                    <div class="customers-show-cart-table cart-table hidden">
                      <div class="customers-show-cart-header">
                        <h5 class="customers-show-cart-title">Itens do Carrinho</h5>
                      </div>
                      <div class="customers-show-cart-content">
                        <% cart = JSON.parse(sale.cart) %>
                        <% cart.each do |item| %>
                          <div class="customers-show-cart-item">
                            <div class="customers-show-item-name"><%= item["name"] %></div>
                            <div class="customers-show-item-details">
                              <span class="customers-show-item-price"><%= number_to_currency(item["price"]) %></span>
                              <span class="customers-show-item-quantity">Qtd: <%= item["quantity"] %></span>
                            </div>
                          </div>
                        <% end %>
                      </div>
                    </div>
                  </div>
                <% end %>
              </div>
            <% else %>
              <div class="customers-show-empty-state">
                <i class="bi bi-cart-x customers-show-empty-icon"></i>
                <h4 class="customers-show-empty-title">Nenhuma compra realizada</h4>
                <p class="customers-show-empty-text"><%= I18n.t(:no_purchases_made) %></p>
              </div>
            <% end %>
          </div>
        </div>
      </div>

      <!-- Documents Tab -->
      <div class="customers-show-tab-pane fade" id="documents-tab-pane" role="tabpanel" aria-labelledby="documents-tab" tabindex="0">
        <div class="customers-show-documents-card">
          <div class="customers-show-documents-header">
            <h3 class="customers-show-documents-title">
              <i class="bi bi-files customers-show-documents-icon"></i>
              Documentos
            </h3>
          </div>
          <div class="customers-show-documents-content">
            <div class="customers-show-empty-state">
              <i class="bi bi-file-earmark-plus customers-show-empty-icon"></i>
              <h4 class="customers-show-empty-title">Funcionalidade em desenvolvimento</h4>
              <p class="customers-show-empty-text">Em breve você poderá visualizar e gerenciar documentos do cliente.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
